generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  name      String
  password  String
  role      String   @default("editor") // admin, editor
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  sites     SiteUser[]
}

model Site {
  id           Int           @id @default(autoincrement())
  name         String
  slug         String        @unique
  description  String?
  status       String        @default("active")
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt
  users        SiteUser[]
  pages        Page[]
  posts        Post[]
  media        Media[]
  siteFeatures SiteFeature[]
  featureEntries FeatureEntry[]
  home         SiteHome?
  announcements Announcement[]
  progressItems ProgressItem[]
  exhibitions Exhibition[]
  meetings     MeetingRecord[]
  vendors      Vendor[]
  selection    SelectionInfo?
  maps         MapAsset[]
  attachments  ContentAttachment[]
}

model FeatureDefinition {
  id           Int           @id @default(autoincrement())
  key          String        @unique
  label        String
  icon         String?
  path         String
  description  String?
  isSystem     Boolean       @default(false)
  displayMode  String        @default("list")
  createdAt    DateTime      @default(now())
  siteFeatures SiteFeature[]
  entries      FeatureEntry[]
}

model SiteFeature {
  id          Int               @id @default(autoincrement())
  siteId      Int
  featureId   Int
  enabled     Boolean           @default(true)
  sortOrder   Int               @default(0)
  displayMode String            @default("list")
  visibility  String            @default("public")
  site        Site              @relation(fields: [siteId], references: [id], onDelete: Cascade)
  feature     FeatureDefinition @relation(fields: [featureId], references: [id], onDelete: Cascade)

  @@unique([siteId, featureId])
}

model FeatureEntry {
  id          Int               @id @default(autoincrement())
  siteId      Int
  featureId   Int
  title       String
  content     String?
  contentType String            @default("text")
  youtubeUrl  String?
  mediaId     Int?
  createdAt   DateTime          @default(now())
  updatedAt   DateTime          @updatedAt
  site        Site              @relation(fields: [siteId], references: [id], onDelete: Cascade)
  feature     FeatureDefinition @relation(fields: [featureId], references: [id], onDelete: Cascade)
  media       Media?            @relation("FeatureEntryMedia", fields: [mediaId], references: [id], onDelete: Restrict)

  @@index([siteId, featureId])
  @@index([mediaId])
}

model SiteUser {
  id        Int      @id @default(autoincrement())
  userId    Int
  siteId    Int
  role      String   @default("editor") // admin, editor, viewer
  createdAt DateTime @default(now())
  user      User     @relation(fields: [userId], references: [id])
  site      Site     @relation(fields: [siteId], references: [id])

  @@unique([userId, siteId])
}

model Page {
  id        Int      @id @default(autoincrement())
  siteId    Int
  title     String
  slug      String
  content   String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  site      Site     @relation(fields: [siteId], references: [id])

  @@unique([siteId, slug])
}

model Post {
  id        Int      @id @default(autoincrement())
  siteId    Int
  title     String
  slug      String
  content   String?
  published Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  site      Site     @relation(fields: [siteId], references: [id])

  @@unique([siteId, slug])
}

model Media {
  id        Int      @id @default(autoincrement())
  siteId    Int
  filename  String
  url       String
  type      String
  mimeType  String?
  sizeBytes Int?
  altText   String?
  createdAt DateTime @default(now())
  site      Site     @relation(fields: [siteId], references: [id])
  attachments ContentAttachment[]
  homeHero   SiteHome[] @relation("SiteHomeHeroMedia")
  vendorLogos Vendor[] @relation("VendorLogoMedia")
  mapImages MapAsset[] @relation("MapImageMedia")
  mapDownloads MapAsset[] @relation("MapDownloadMedia")
  featureEntryImages FeatureEntry[] @relation("FeatureEntryMedia")
}

model SiteHome {
  id             Int      @id @default(autoincrement())
  siteId         Int      @unique
  tagline        String?
  intro          String?
  heroMediaId    Int?
  currentStage   String?
  contactName    String?
  contactPhone   String?
  contactEmail   String?
  contactAddress String?
  updatedAt      DateTime @updatedAt
  site           Site     @relation(fields: [siteId], references: [id], onDelete: Cascade)
  heroMedia      Media?   @relation("SiteHomeHeroMedia", fields: [heroMediaId], references: [id], onDelete: SetNull)
}

model Announcement {
  id          Int      @id @default(autoincrement())
  siteId      Int
  title       String
  slug        String
  summary     String?
  content     String?
  category    String?
  pinned      Boolean  @default(false)
  status      String   @default("draft")
  publishedAt DateTime?
  sortOrder   Int      @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  site        Site     @relation(fields: [siteId], references: [id], onDelete: Cascade)

  @@unique([siteId, slug])
  @@index([siteId])
}

model ProgressItem {
  id             Int      @id @default(autoincrement())
  siteId         Int
  stageDate      DateTime
  stageLabel     String
  title          String
  summary        String?
  content        String?
  progressStatus String
  status         String   @default("draft")
  sortOrder      Int      @default(0)
  publishedAt    DateTime?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  site           Site     @relation(fields: [siteId], references: [id], onDelete: Cascade)

  @@index([siteId])
}

model Exhibition {
  id              Int      @id @default(autoincrement())
  siteId          Int
  title           String
  slug            String
  startDate       DateTime?
  endDate         DateTime?
  location        String?
  description     String?
  feedbackSummary String?
  status          String   @default("draft")
  sortOrder       Int      @default(0)
  publishedAt     DateTime?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  site            Site     @relation(fields: [siteId], references: [id], onDelete: Cascade)

  @@unique([siteId, slug])
  @@index([siteId])
}

model MeetingRecord {
  id          Int      @id @default(autoincrement())
  siteId      Int
  meetingType String
  meetingNo   String?
  title       String
  meetingDate DateTime
  summary     String?
  status      String   @default("draft")
  sortOrder   Int      @default(0)
  publishedAt DateTime?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  site        Site     @relation(fields: [siteId], references: [id], onDelete: Cascade)

  @@index([siteId])
}

model Vendor {
  id           Int      @id @default(autoincrement())
  siteId       Int
  name         String
  category     String
  summary      String?
  description  String?
  services     String?
  contactName  String?
  contactPhone String?
  contactEmail String?
  logoMediaId  Int?
  status       String   @default("draft")
  sortOrder    Int      @default(0)
  publishedAt  DateTime?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  site         Site     @relation(fields: [siteId], references: [id], onDelete: Cascade)
  logoMedia    Media?   @relation("VendorLogoMedia", fields: [logoMediaId], references: [id], onDelete: SetNull)

  @@index([siteId])
}

model SelectionInfo {
  id             Int      @id @default(autoincrement())
  siteId         Int      @unique
  title          String
  description    String?
  applicableStage String?
  rules          String?
  notice         String?
  deadline       DateTime?
  externalUrl    String?
  status         String   @default("draft")
  publishedAt    DateTime?
  updatedAt      DateTime @updatedAt
  site           Site     @relation(fields: [siteId], references: [id], onDelete: Cascade)
}

model MapAsset {
  id             Int      @id @default(autoincrement())
  siteId         Int
  title          String
  category       String?
  description    String?
  imageMediaId   Int?
  downloadMediaId Int?
  status         String   @default("draft")
  sortOrder      Int      @default(0)
  publishedAt    DateTime?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  site           Site     @relation(fields: [siteId], references: [id], onDelete: Cascade)
  imageMedia     Media?   @relation("MapImageMedia", fields: [imageMediaId], references: [id], onDelete: SetNull)
  downloadMedia  Media?   @relation("MapDownloadMedia", fields: [downloadMediaId], references: [id], onDelete: SetNull)

  @@index([siteId])
}

model ContentAttachment {
  id        Int      @id @default(autoincrement())
  siteId    Int
  mediaId   Int
  ownerType String
  ownerId   Int
  label     String?
  sortOrder Int      @default(0)
  createdAt DateTime @default(now())
  site      Site     @relation(fields: [siteId], references: [id], onDelete: Cascade)
  media     Media    @relation(fields: [mediaId], references: [id], onDelete: Cascade)

  @@index([siteId])
  @@index([ownerType, ownerId])
}
